name: flutter_practical1
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  http: ^0.12.2
  flutter_spinkit: ^4.1.2+1
  provider: ^4.3.3
  hive: ^1.4.1+1
  path_provider: ^1.6.5
  hive_flutter: ^0.3.0+2




dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
